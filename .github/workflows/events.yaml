name: events
on:
  pull_request:
    types:
      - opened
  workflow_dispatch:
  push:
    # Event filters:
    branches:
      - main
      - "dev/*"
    paths-ignore:
      - ".github/workflows/*"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Lint code
        run: npm run lint
      - name: Test code
        run: npm run test
      - name: Build code
        run: npm run build
      